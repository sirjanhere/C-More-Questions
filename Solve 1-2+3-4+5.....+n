#include <iostream>
using namespace std;

int seriesSum(int n) {
    if (n % 2 == 0) {
        return -(n / 2);
    } else {
        return (n + 1) / 2;
    }
}

int main() {
    int n;
    cout << "Enter n: ";
    cin >> n;
    cout << "Sum of series: " << seriesSum(n) << endl;
    return 0;
}
